$(document).ready(function () {
  // AirTicket
  $(".AirTicket-tab").simpleTab({
    defaultSkin: true,
    fadeEffect: true,
  });

  // packages
  $(".packages-tab").simpleTab({
    defaultSkin: true,
    fadeEffect: true,
  });
});
